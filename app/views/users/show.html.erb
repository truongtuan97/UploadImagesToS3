<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Avatar:</strong>  
</p>

<p>
	<strong><em>original size</em></strong>
	<%= image_tag(@user.avatar) %>
</p>

<p>
	<strong><em>small size</em></strong>
	<%= image_tag(@user.avatar.small_avatar) %>
</p>

<p>
	<strong><em>medium size</em></strong>
	<%= image_tag(@user.avatar.medium_avatar) %>
</p>

<p>
	<strong><em>large size</em></strong>
	<%= image_tag(@user.avatar.large_avatar) %>
</p>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
